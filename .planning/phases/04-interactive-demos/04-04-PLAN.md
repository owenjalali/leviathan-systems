---
phase: 04-interactive-demos
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/sections/home/DemoSection.jsx
  - src/pages/Home.jsx
autonomous: false

must_haves:
  truths:
    - "All three demo parts render on the homepage in order: Part I, Part II, Part III"
    - "Each part is a distinct section with clear visual separation and section headers"
    - "Demo intro text ('See the System in Action') appears before Part I"
    - "All three parts auto-play independently when they scroll into viewport"
    - "Navigate away and back — no GSAP memory leaks, animations restart cleanly"
    - "Mobile responsive — all three parts render correctly on small screens"
    - "Build succeeds with no warnings"
  artifacts:
    - path: "src/sections/home/DemoSection.jsx"
      provides: "Rewritten demo wrapper rendering intro + all three demo parts"
      min_lines: 30
    - path: "src/pages/Home.jsx"
      provides: "Updated homepage composition with new demo sections"
  key_links:
    - from: "src/sections/home/DemoSection.jsx"
      to: "src/sections/home/DemoPartOne.jsx"
      via: "imports and renders DemoPartOne"
      pattern: "import DemoPartOne"
    - from: "src/sections/home/DemoSection.jsx"
      to: "src/sections/home/DemoPartTwo.jsx"
      via: "imports and renders DemoPartTwo"
      pattern: "import DemoPartTwo"
    - from: "src/sections/home/DemoSection.jsx"
      to: "src/sections/home/DemoPartThree.jsx"
      via: "imports and renders DemoPartThree"
      pattern: "import DemoPartThree"
    - from: "src/pages/Home.jsx"
      to: "src/sections/home/DemoSection.jsx"
      via: "renders DemoSection in homepage flow"
      pattern: "<DemoSection"
---

<objective>
Integrate all three demo parts into a cohesive demo zone on the homepage. Rewrite DemoSection.jsx to render the intro text followed by Parts I, II, and III as distinct sections. Update Home.jsx if needed. Verify the full demo flow, mobile responsiveness, and GSAP cleanup.

Purpose: This is the integration plan that ties together the three independently-built demo parts into one cohesive experience on the homepage. The demo is the centerpiece of the site.

Output: Rewritten DemoSection.jsx, updated Home.jsx, verified cohesive demo experience
</objective>

<execution_context>
@C:/Users/owenj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/owenj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-interactive-demos/04-CONTEXT.md
@.planning/phases/04-interactive-demos/04-01-SUMMARY.md
@.planning/phases/04-interactive-demos/04-02-SUMMARY.md
@.planning/phases/04-interactive-demos/04-03-SUMMARY.md
@src/pages/Home.jsx
@src/sections/home/DemoSection.jsx
@src/content/home.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DemoSection and integrate into Home.jsx</name>
  <files>src/sections/home/DemoSection.jsx, src/pages/Home.jsx</files>
  <action>
**Rewrite DemoSection.jsx:**

Replace the current Timeline-based placeholder DemoSection with a new version that renders the three demo parts:

1. Remove the Timeline component import and timeline data mapping.
2. Import DemoPartOne, DemoPartTwo, DemoPartThree from the same directory.
3. Import demoIntro from `src/content/home.js`.

Structure:
```
<section> (outer wrapper, no specific bg — each part handles its own)
  <!-- Demo intro -->
  <div> centered intro block
    <h2> "See the System in Action." </h2>
    <p> "One customer request. Three perspectives..." </p>
  </div>

  <!-- Three distinct parts -->
  <DemoPartOne />
  <DemoPartTwo />
  <DemoPartThree />
</section>
```

- The intro block should use the same styling as the current DemoSection intro (text-center, max-w-2xl mx-auto, clamp font size, etc.)
- Each DemoPartX component is a self-contained section with its own padding, background, and ScrollTrigger — they are distinct sections, not a continuous flow (per CONTEXT.md locked decision)
- Ensure clear visual separation between parts. Each part already has its own section header ("Part I: Customer Experience", etc.). Add spacing between them (py-16 or py-20 between parts, or let each part's internal padding handle it).
- The intro block should have standard section padding (use SectionWrapper or equivalent).

**Update Home.jsx (if needed):**

Home.jsx currently renders `<DemoSection />` in position 4. This should still work since we're rewriting DemoSection in-place. Verify the import still works. If the component signature changed, update accordingly.

No new imports should be needed in Home.jsx — DemoSection remains the single import, and it internally composes the three parts.

**Cleanup unused files:**
- DemoIntroSection.jsx and DemoPlaceholder.jsx are NOT used in Home.jsx and are leftovers from Phase 3. Do NOT delete them in this plan — they are harmless and may serve as reference. (If the user wants cleanup, that's Phase 5.)

**Visual cohesion (Claude's discretion):**
- Consider adding a subtle background treatment to distinguish the "demo zone" from surrounding sections. Options: very subtle gradient overlay, slightly different bg, thin accent-colored divider line at the top of the demo zone. Keep it understated — the demos themselves are the visual interest.
- Ensure the three parts feel like one cohesive story even though they are distinct sections. The shared narrative (Sarah Mitchell's request) ties them together.
  </action>
  <verify>
Run `npm run build` — no errors or warnings. Run `npm run dev`:
1. Scroll through the full homepage — demo intro appears, then Part I, Part II, Part III in order
2. Each part animates independently when scrolling into view
3. Navigate to /about (or any other route) and back to / — no console errors, no leaked animations, demos replay correctly
4. Resize browser to mobile width — all three parts render correctly, no overflow or broken layouts
5. Check DevTools console for any GSAP warnings or errors
  </verify>
  <done>
DemoSection.jsx is rewritten to compose intro + three demo parts. Home.jsx renders the complete demo flow. All three parts auto-play independently on viewport entry. No GSAP memory leaks on navigation. Mobile responsive. Build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete demo flow</name>
  <what-built>The complete three-part interactive demo — the centerpiece of the site. Part I shows a form auto-filling and SMS conversation appearing. Part II shows a node graph with animated connections. Part III shows an owner dashboard with activity feed and approval buttons.</what-built>
  <how-to-verify>
1. Open http://localhost:5173 in browser
2. Scroll down to the demo zone (after "It's Not You. It's Missing Infrastructure.")
3. Verify "See the System in Action" intro text appears
4. Continue scrolling — Part I should auto-play: form fields fill in, then SMS bubbles appear on the phone mockup
5. Continue scrolling — Part II should auto-play: node graph connections draw sequentially between nodes
6. Continue scrolling — Part III should auto-play: dashboard window frame appears, activity feed items enter one by one, approval card appears
7. Scroll back up through all three parts — animations should NOT replay
8. Resize browser to ~375px width (mobile) — verify all three parts render correctly without horizontal overflow
9. Open DevTools > Console — verify no errors or GSAP warnings
10. Navigate to /about and back to / — verify no console errors (memory leak check)

**Things to look for:**
- Does the phone mockup feel premium (realistic iPhone frame)?
- Does the node graph look clean and minimal (Linear/Figma-style)?
- Does the dashboard window frame look like a real app?
- Is the visual separation between parts clear?
- Does the overall demo feel like one cohesive story (Sarah Mitchell's request from three perspectives)?
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors or warnings
- DemoSection.jsx imports and renders DemoPartOne, DemoPartTwo, DemoPartThree
- Home.jsx still renders DemoSection in position 4 (between ReframeSection and PillarsSection)
- Demo intro text appears before Part I
- Each part is visually distinct with clear separation
- All GSAP animations clean up on navigation (no memory leaks)
- No console errors on any page
- Mobile responsive at 375px width
</verification>

<success_criteria>
1. DemoSection.jsx composes intro + three demo parts
2. All three parts render in order on the homepage
3. Each part auto-plays independently on viewport scroll-in
4. Parts are visually distinct sections with clear separation
5. Navigation away and back causes no GSAP memory leaks
6. Mobile responsive — no overflow or broken layouts
7. No console errors or GSAP warnings
8. `npm run build` passes with no warnings
9. User approves the visual quality and cohesion of the complete demo
</success_criteria>

<output>
After completion, create `.planning/phases/04-interactive-demos/04-04-SUMMARY.md`
</output>
