---
phase: 04-interactive-demos
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/WindowFrame.jsx
  - src/sections/home/DemoPartThree.jsx
autonomous: true

must_haves:
  truths:
    - "User sees a macOS-style window frame containing an activity feed"
    - "Activity feed shows what the system handled (Sarah Mitchell's request flow)"
    - "Approve and Override buttons are visible and styled as interactive"
    - "Feed items animate in sequentially on viewport entry"
    - "Animation plays once on viewport entry, does not replay on scroll back"
    - "prefers-reduced-motion shows static completed state"
    - "Dashboard emphasizes human-safe autonomy — system handled everything, owner just approves"
  artifacts:
    - path: "src/components/ui/WindowFrame.jsx"
      provides: "Reusable macOS-style window chrome component with title bar and traffic light dots"
      min_lines: 25
    - path: "src/sections/home/DemoPartThree.jsx"
      provides: "Complete Demo Part III section with GSAP animated activity feed"
      min_lines: 80
  key_links:
    - from: "src/sections/home/DemoPartThree.jsx"
      to: "src/content/demo-data.js"
      via: "imports approvalAction, smsConversation or dashboardMetrics"
      pattern: "import.*approvalAction.*demo-data"
    - from: "src/sections/home/DemoPartThree.jsx"
      to: "src/components/ui/WindowFrame.jsx"
      via: "renders WindowFrame component"
      pattern: "<WindowFrame"
    - from: "src/sections/home/DemoPartThree.jsx"
      to: "gsap/ScrollTrigger"
      via: "useGSAP with ScrollTrigger once:true"
      pattern: "once:\\s*true"
---

<objective>
Build Demo Part III: Owner Dashboard — a macOS window frame mockup containing an activity feed that shows what the system just handled, with approve/override buttons. Animated on viewport entry using GSAP.

Purpose: This is the third demo section, showing the owner's perspective. The system handled Sarah Mitchell's request automatically — the owner sees a clean activity feed and can approve or override with one tap. This reinforces the "human-safe autonomy" pillar: systems operate independently while humans stay informed.

Output: WindowFrame.jsx (reusable UI component), DemoPartThree.jsx (section component with GSAP animations)
</objective>

<execution_context>
@C:/Users/owenj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/owenj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-interactive-demos/04-CONTEXT.md
@.planning/phases/04-interactive-demos/04-RESEARCH.md
@src/content/demo-data.js
@src/content/home.js
@src/components/ui/BackgroundPaths.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WindowFrame component and DemoPartThree section</name>
  <files>src/components/ui/WindowFrame.jsx, src/sections/home/DemoPartThree.jsx</files>
  <action>
**WindowFrame.jsx — Generate using 21st.dev Magic component builder:**

Use `mcp__magic__21st_magic_component_builder` to generate a macOS-style window frame component. Prompt it for: "A macOS window chrome component with title bar containing red/yellow/green traffic light dots, a title text, and a content area below. Dark theme. Subtle border and shadow. The content area renders children." After generation, customize:
- Accept `title` prop (string, displayed in title bar, default "Owner Dashboard")
- Accept `children` prop for window content
- Accept optional `className` prop
- Title bar: dark background (~#2a2a2a or --bg-secondary), traffic light dots (red #ff5f56, yellow #ffbd2e, green #27c93f), title text in --text-secondary
- Window body: --bg-secondary background with padding
- Border: --border color, rounded-lg corners
- Subtle shadow (shadow-2xl or similar) for depth

**DemoPartThree.jsx — Build the complete section:**

1. **Section header:** "Part III: Owner Dashboard" using demoParts[2] from `src/content/home.js`. Same header style pattern as Parts I and II.

2. **Dashboard layout inside WindowFrame:**
   The window frame contains an activity feed. This is NOT a metrics dashboard. Per CONTEXT.md locked decision: "Activity feed showing what the system just handled, with one-tap approve/override buttons. Emphasizes human-safe autonomy."

   Build the activity feed with these items (derive from demo-data.js narrative):
   - Feed item 1: "New lead received" — Sarah Mitchell submitted a request (timestamp: "2 min ago")
   - Feed item 2: "Lead qualified" — Automatically verified contact info and service type (timestamp: "2 min ago")
   - Feed item 3: "Request routed" — Matched to available appointment slot (timestamp: "1 min ago")
   - Feed item 4: "SMS sent" — Booking confirmation sent to Sarah Mitchell (timestamp: "1 min ago")
   - Feed item 5: "Appointment booked" — Tuesday at 2pm confirmed (timestamp: "Just now")

   Each feed item should have:
   - A small status indicator (green dot or checkmark icon from lucide-react)
   - The action description
   - A muted timestamp on the right
   - Styled with --text-primary for text, --text-secondary for timestamp, --border for separator lines

3. **Approval action (prominent):**
   Import `approvalAction` from demo-data.js.
   Below the activity feed, render a highlighted approval card:
   - Text: "Approve meeting with Sarah Mitchell — Tuesday at 2pm"
   - Two buttons: "Approve" (accent/teal styled, filled) and "Override" (outline/ghost styled)
   - This is the punchline — the system did everything, the owner just confirms
   - Buttons are decorative (not functional) but styled to look interactive

4. **GSAP Animation (viewport-triggered):**
   - Use `useGSAP` hook with `{ scope: containerRef }`
   - Register ScrollTrigger: `gsap.registerPlugin(ScrollTrigger)`
   - Create timeline with `scrollTrigger: { trigger: containerRef.current, start: "top 75%", once: true }`
   - Wrap in `gsap.matchMedia()`:
     - `(prefers-reduced-motion: no-preference)`: Full animation sequence:
       a. Window frame fades in and slides up slightly (opacity 0->1, y 20->0, duration 0.5s)
       b. Feed items appear one by one with stagger (0.2s between each, fade + slide from left)
       c. After all feed items shown, the approval card slides in from bottom with a slight scale effect (0.95->1)
       d. Approve button gets a subtle pulse/glow effect to draw attention
       e. Total animation: ~4-5 seconds
     - `(prefers-reduced-motion: reduce)`: All content visible immediately (opacity fade 0.3s)

5. **Layout:** Center the WindowFrame in the section with max-w-2xl or similar constraint. The dashboard should feel contained and app-like, not stretched across the full page width.

6. **Background:** Use --bg-primary. Subtle visual distinction (Claude's discretion).
  </action>
  <verify>
Run `npm run build` from project root — no errors. Run `npm run dev` and scroll to Demo Part III section. Verify:
- Window frame mockup with traffic light dots and "Owner Dashboard" title
- Activity feed shows 5 items appearing one by one
- Approval action card with Approve/Override buttons appears after feed
- Animation plays once and does not replay
- Dashboard feels like "the system handled it, owner just confirms"
  </verify>
  <done>
DemoPartThree.jsx renders a complete Part III section with macOS window frame containing animated activity feed (5 items showing Sarah Mitchell's request flow) and approval action with Approve/Override buttons. Feed items animate in sequentially on viewport entry. The section reinforces human-safe autonomy. prefers-reduced-motion shows static state. Build succeeds.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- DemoPartThree.jsx imports from demo-data.js (approvalAction) and home.js (demoParts)
- WindowFrame.jsx is a reusable component accepting title and children
- Activity feed shows 5 chronological items from the Sarah Mitchell narrative
- Approve/Override buttons are styled but decorative (not functional)
- GSAP ScrollTrigger with `once: true` triggers on viewport entry
- gsap.matchMedia() handles prefers-reduced-motion
- useGSAP hook with scope provides automatic cleanup
</verification>

<success_criteria>
1. WindowFrame.jsx exists as a reusable macOS window chrome component
2. DemoPartThree.jsx renders activity feed inside window frame
3. Feed items appear sequentially on viewport entry
4. Approval action card with Approve/Override buttons is prominent
5. Dashboard emphasizes autonomy: system handled everything, owner just approves
6. Animation plays once (once: true), does not replay
7. prefers-reduced-motion shows all content instantly without motion
8. `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-interactive-demos/04-03-SUMMARY.md`
</output>
